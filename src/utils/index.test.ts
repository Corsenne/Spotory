import { describe,expect,it } from 'vitest'; import { canEdit,googleMapsUrl,resizeDimensions,validateVisit,visitsToCsv } from '.'; import type { Visit,VisitFormValue } from '../../src/types';
describe('utilities',()=>{it('Place IDを含むGoogle Maps URLを生成する',()=>{const url=googleMapsUrl({name:'店',google_place_id:'abc 123'});expect(url).toContain('query_place_id=abc%20123')});it('経路URLには座標を使う',()=>{expect(googleMapsUrl({latitude:35,longitude:139},true)).toContain('destination=35%2C139')});it('縦横比を保って1600pxに縮小する',()=>{expect(resizeDimensions(4000,3000)).toEqual({width:1600,height:1200})});it('公開範囲とグループを検証する',()=>{const value={place:{name:'店'},visited_at:'2026-07-21',rating:5,comment:'',visibility:'group',is_favorite:false,is_wishlist:false,category:'',tags:[],group_ids:[]} satisfies VisitFormValue;expect(validateVisit(value)).toContain('共有グループを選択してください')});it('editorは編集できviewerはできない',()=>{expect(canEdit('a','b','editor')).toBe(true);expect(canEdit('a','b','viewer')).toBe(false)});it('CSVをBOM付きでエスケープする',()=>{const visit={id:'1',place_id:'p',user_id:'u',visited_at:'2026-01-01',rating:4,comment:'a,"b"',visibility:'private',is_favorite:false,is_wishlist:false,created_at:'',place:{id:'p',name:'店',created_by:'u',created_at:''}} satisfies Visit;const csv=visitsToCsv([visit]);expect(csv.charCodeAt(0)).toBe(0xfeff);expect(csv).toContain('"a,""b"""')})});
